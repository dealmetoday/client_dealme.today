import { Map } from 'immutable'

/**
 * The initial values for the redux state.
 */
export const INITIAL_STATE = {
  user: null,
  signInMethod: null,
  config: null,
  profile: null,
  isLoggedIn: false,
  pubKey: null
}
